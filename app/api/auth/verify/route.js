import { ok } from '@/lib/utils';

export async function POST() {
  return ok({ message: 'Email verification is no longer required' });
}

export async function PUT() {
  return ok({ message: 'Email verification is no longer required' });
}
