export const dynamic = 'force-dynamic';

export const metadata = {
  title: { absolute: 'Nitro' },
};

export default function DashboardLayout({ children }) {
  return children;
}
// Build cache bust: 1775641089
